const state = {
  transaction: [
    {
      id: 1,
      timeout: 1200,
      address: "Jumah",
      regency: "Gianyar",
      province: "Bali",
      sub_total: 15000,
      shipping_cost: 5000,
      total: 5000
    }
  ]
};

const getters = {};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations
};
